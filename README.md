# model-archaeologist
model_archaeologist is a CLI tool that reverse-engineers public AI models by ingesting their published papers, blog posts, and benchmark results, then using an LLM to hypothesize architectural design decisions. Given a model name or a set of URLs/text files, it produces a structured Markdown report covering likely architecture patterns (attention m
